apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - postgres-statefulset.yaml
  - postgres-service.yaml
  # Ingress removed for internal-only exposure
namespace: blinko


