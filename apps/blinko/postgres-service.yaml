apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: blinko
  labels:
    app: blinko-postgres
spec:
  type: ClusterIP
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: blinko-postgres


