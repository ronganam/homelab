apiVersion: v1
kind: Service
metadata:
  name: kan-postgres
  namespace: kan
  labels:
    app: kan-postgres
spec:
  type: ClusterIP
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: kan-postgres
