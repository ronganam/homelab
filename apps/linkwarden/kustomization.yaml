# Kustomization for INTERNAL app
# Use this for apps that should be accessible via ingress
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
namespace: linkwarden
