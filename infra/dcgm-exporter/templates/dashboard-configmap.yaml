apiVersion: v1
kind: ConfigMap
metadata:
  name: dcgm-exporter-grafana-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  nvidia-dcgm-exporter.json: |
    {{- .Files.Get "dashboards/nvidia-dcgm-exporter.json" | nindent 4 }}
